import java.util.*;

public class Main{
    public static void main(String[] args) {
        Scanner scan = new Scanner(System.in) ;

        // Integer N number of Messages to be sent

        System.out.println("Enter the number of messages to be sent :");
        int n = scan.nextInt(); 

        while(n-- >0){
            int senderGroundStation = scan.nextInt();  
            int recevierGroundStation = scan.nextInt();
        }
        

        scan.close();
    }
}